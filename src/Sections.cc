#include "includes/Sections.hh"

namespace libelfparser {
    Sections::Sections(void *mappedMem)
	{

	}

    std::size_t Sections::count() const
	{

	}

    std::shared_ptr<ISection> Sections::operator[](std::size_t n) const
	{

	}
}
